
from typing import Optional

        
class TreeNode:
     def __init__(self, val=0, left=None, right=None):
         self.val = val
         self.left = left
         self.right = right
class Solution:
    def checkTree(self, root: Optional[TreeNode]) -> bool:
        ""
        stdin = input(": ")
        root = [].append(stdin)
        
        for i in stdin:
            if stdin == stdin+stdin:
                print(root)
            else:
                print(root)

Solution.checkTree(self=None, root=None)