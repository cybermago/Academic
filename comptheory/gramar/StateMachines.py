import numpy as np
import re

#Problema: Programe um simulador que determine se uma palavra é aceita por um AFD.
#Contexto Teórico: Um AFD é um modelo matemático que reconhece linguagens, composto por estados, transições e um estado inicial/final.


#Tarefa:
#    Represente o AFD como uma estrutura de dados (ex: um dicionário para as transições).
#    Crie uma função simula_afd(automato, palavra) que, a partir do estado inicial, percorre as transições para cada símbolo da palavra.
#    A função deve retornar True se o estado final for alcançado após o processamento da palavra, e False caso contrário.

class StateMachines:
    def __init__(self):
        pass

    def afd(auto, word):
        "Simulador de Automatos"
        "Args: auto, word"
        "Returns: True -> FinalState False"
        pass

    def nfd(aut, word):
        ""
        pass