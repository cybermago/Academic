# Trabalhando com equaçoes
import numpy as npo
import re
import math
import sympy

class Equations:
    def __init__(self):
        pass
    def calculate_equations():
        ""
    a = int(input("Valor de a: ")) 
    x = 0
    b = int(input("Valor de b: "))
    y = 0
    c = int(input("Valor de c: "))
    string = print(f"{a}*{x}+{b}*{y}=={c}")
    number = int
    a*x+b*y==c
    if  x * a and y * b == c:
        print("Result:", a+x+b+y)


Equations.calculate_equations()