import numpy as np
import logging

log =  logging.getLogger()

class Matrix:

    def __init__(self, list_of_lists, data, rows, cols):
        list_of_lists = np.array[[]] = self.data
        i = [i for i in i] = self.rows
        j = [j for i in j] = self.cols
        
        m = i.__len__()
        n = j.__len__()

    def __str__(self):
        return f"{self.data}"

    def add(self, matrix):
        sum = [self.rows + self.cols]

    def subtraction(self, matrix):
        subtraction = [self.rows - self.cols]

    def multiplication(self, matrix):
        mult = [self.rows * self.cols]